# Backend-only dependencies (no torch/transformers/gradio — those are for local training only)

# Data loading (for corpus/dictionary)
numpy>=2.0,<2.3
pandas>=2.2
openpyxl>=3.1

# Document parsing / OCR
pymupdf>=1.24.0
pytesseract>=0.3.10
pillow>=10.0.0

# OpenAI API
openai>=1.40.0
