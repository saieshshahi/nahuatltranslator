# Copy this file to .env and fill in your key.
#
# IMPORTANT: Do NOT commit .env files containing secrets.

# OpenAI API key (optional). Enables:
# - "OpenAI only" translation
# - "Auto" fallback when the local model output looks degenerate
# - Instruction-following extraction
OPENAI_API_KEY=sk-your-key-here

# Optional: override models used for fallback
OPENAI_TRANSLATE_MODEL=gpt-4o-mini
OPENAI_EXTRACT_MODEL=gpt-4o-mini

# Manuscript transcription (vision)
OPENAI_TRANSCRIBE_MODEL=gpt-4o-mini

# Optional default temperatures
OPENAI_TRANSLATE_TEMPERATURE=0.2
OPENAI_TRANSCRIBE_TEMPERATURE=0.0

# Optional: if you're using a compatible endpoint
# OPENAI_BASE_URL=
# OPENAI_ORG=
# OPENAI_PROJECT=
